# ── Core API ──────────────────────────────────────────────────
fastapi==0.115.12
uvicorn[standard]==0.34.2
pydantic==2.11.5
python-dotenv==1.1.0
python-multipart==0.0.20
websockets>=13
httpx>=0.27

# ── Dashboard ─────────────────────────────────────────────────
streamlit==1.45.1

# ── Data / Artifacts ──────────────────────────────────────────
requests==2.32.3
pandas==2.2.3
openpyxl==3.1.5

# ── LangGraph / LangChain (cognitive orchestration) ──────────
langgraph>=0.2.50
langchain>=0.3
langchain-core>=0.3
langchain-community>=0.3
langchain-ollama>=0.2

# ── Memory: SQLite async (lightweight, zero-infra) ────────────
sqlalchemy[asyncio]>=2.0
aiosqlite>=0.20

# ── Memory: Redis (short-term, optional) ─────────────────────
redis>=5.0

# ── Browser automation ───────────────────────────────────────
playwright>=1.44.0
beautifulsoup4>=4.12
lxml>=5.0

# ── Metrics ───────────────────────────────────────────────────
prometheus-client>=0.20

# ── Logging ───────────────────────────────────────────────────
structlog>=24

# ── AWS (optional Bedrock) ───────────────────────────────────
boto3>=1.38

# ── Testing ───────────────────────────────────────────────────
pytest>=8.0
pytest-asyncio>=0.23
aiofiles>=23.0
stripe>=10.0
# httpx for telegram bot
httpx>=0.27
aiofiles>=23.0
